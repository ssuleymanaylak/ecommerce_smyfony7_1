{% extends 'base.html.twig'%}
{% block body %}
<div class="container">
    <div class="form">
        <h2>Update a category</h2> <br> <br>
        {{ form_start(form) }}
            {{ form_row(form.name,{'attr':{'class':'form form-control'}}) }}
            <br>
            <input type='submit' value="Update" class="btn btn-primary">
        {{ form_end(form) }}
    </div>

</div>

{% endblock %}